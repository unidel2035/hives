import { createOpenAICompatible } from "@ai-sdk/openai-compatible"; const polza = createOpenAICompatible({ name: "polza", baseURL: "https://api.polza.ai/api/v1", apiKey: "ak_0xCOU-hEsCsImB6r-dg7GChm2LFPQOUL9ROwExY8WBo" }); const model = polza.languageModel("anthropic/claude-sonnet-4.5"); console.log("Model methods:", Object.getOwnPropertyNames(Object.getPrototypeOf(model)).filter(name => typeof model[name] === 'function')); const result = await model.doGenerate({ prompt: [{ role: "user", content: [{ type: "text", text: "Hello from polza!" }] }] }); console.log("✅ Polza API call successful"); console.log("Full response:", JSON.stringify(result, null, 2));
